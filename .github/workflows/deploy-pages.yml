name: Deploy PUiCE 2026 to GitHub Pages

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build static GitHub Pages site
        env:
          GITHUB_PAGES: 'true'
          NEXT_PUBLIC_BASE_PATH: /puice2026utama
          NEXT_PUBLIC_SITE_URL: https://ustppu.github.io/puice2026utama
          NEXT_PUBLIC_PUICE_API_URL: https://script.google.com/macros/s/AKfycbwwDyQmSEy2O1-cwGQHgFvX7xxxEPCY6Nv9BCGfweWSGrJPVtb33oe68XUAArrSQpsX8w/exec
        run: |
          mv app/api /tmp/puice-server-api
          node node_modules/next/dist/bin/next build
          node scripts/finalize-github-pages.mjs
          node scripts/validate-github-pages.mjs
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
